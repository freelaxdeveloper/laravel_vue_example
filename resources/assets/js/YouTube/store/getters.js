export const searchResults = (state) => state.searchResults
// export const playlists = (state) => state.playlists.filter(x => x.id !== state.selected.id)
export const playlists = (state) => state.playlists
// export const playlistsAll = (state) => state.playlists
export const selected = (state) => state.selected
export const videoInfo = (state) => state.videoInfo
export const token = (state) => state.token
export const user = (state) => state.user
export const showPlaylist = (state) => state.showPlaylist
